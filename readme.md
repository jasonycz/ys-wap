#









## 安装环境
```bash
npm i --save-dev gulp gulp-autoprefixer browser-sync gulp-load-plugins del run-sequence gulp-if gulp-concat gulp-angular-templatecache gulp-rev gulp-rev-collector gulp-uglify gulp-csso gulp-rimraf gulp-rename gulp-clean-css gulp-imagemin gulp-angular-filesort 
```

## 安装依赖js
```
npm i --save angular angular-animate angular-cookies angular-resource angular-route angular-sanitize angular-touch ngstorage moment jquery angular-i18n
```

